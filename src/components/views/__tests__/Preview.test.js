it('if preview page body is showing in the preview page', () => {
    // validate page body is rendered on screen
});

it('if a clock is showing in the preview page body', () => {
    // validate a clock element is showing in the page body
});

it('that a clock is showing in ISO 8601 yyyy-mm-dd format in the preview page body', () => {
    // validate page body is rendered on screen
});